[package]
name = "blueprint"
version = "0.1.0"
edition = "2024"
description = "Blueprint - Platform-agnostic governance framework CLI"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
serde_yml = "0.0.12"
